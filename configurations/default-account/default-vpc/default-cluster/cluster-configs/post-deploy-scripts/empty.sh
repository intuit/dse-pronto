# NOTE: additional post-deploy shell scripts can be placed in this directory...
#    ${account_name}/${vpc_name}/${cluster_name}/cluster-configs/post-deploy-scripts/

# They will be transferred to your cluster at deploy time, and executed at the end of
# the bootstrap.sh script during cloud-init.
